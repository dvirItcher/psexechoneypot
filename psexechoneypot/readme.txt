1. i added PsExec.exe version 2.11
2. change the log file location, and give access only to you
3. compile the file psexecsvc-trap.py into exe, it can be done by the command pyinstaller --onefile --noconsole psexecsvc_trap.py
4. move the compile file into the ADMIN$ location, and rename it to psexecsvc.exe
